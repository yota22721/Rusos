cargo-features = ["per-package-target"]

[package]
name ="libkernel"
version = "0.1.0"
forced-target = "riscv64gc-unknown-none-elf"

[lib]
name = "kernel"
path = "lib.rs"

[features]
default = ["kernel"]
kernel = []